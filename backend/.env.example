# API keys for LLM providers used by agent nodes (optional — tool-only
# workflows run without any keys)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
# Comma-separated origins allowed by CORS
CORS_ORIGINS=http://localhost:5173
